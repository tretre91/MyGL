install_dir = examples_install_dir / 'magnifying_glass'

magnifying_glass = executable('magnifying_glass', 'magnifying_glass.cpp',
  dependencies : mygl_dep,
  install : true,
  install_dir : install_dir,
)

subdir('resources')
