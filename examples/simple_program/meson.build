install_dir = examples_install_dir / 'simple_program'

simple_program = executable('simple_program', 'simple_program.cpp',
  dependencies : mygl_dep,
  install : true,
  install_dir : install_dir,
)

