install_dir = examples_install_dir / 'custom_shader'

custom_shader = executable('custom_shader', 'custom_shader.cpp',
  dependencies : mygl_dep,
  install : true,
  install_dir : install_dir,
)

subdir('resources')
